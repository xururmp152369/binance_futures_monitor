services:
  binance-futures-monitor:
    build: .
    container_name: binance-futures-monitor
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - ./data:/app/data
    ports:
      - "8000:8000"
